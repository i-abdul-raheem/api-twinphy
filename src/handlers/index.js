const { Post } = require("./Post");
const { User } = require("./User");
const { Auth } = require("./Auth");
const { Chat } = require("./Chat");
const { Profile } = require("./Profile");
module.exports = { User, Auth, Chat, Post, Profile };
